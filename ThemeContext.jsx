import React,{createContext,useContext,useEffect,useState} from 'react';const C=createContext(null);export function ThemeProvider({children}){const [theme]=useState('light');useEffect(()=>document.documentElement.setAttribute('data-theme',theme),[theme]);return <C.Provider value={{theme,toggle:()=>{}}}>{children}</C.Provider>}export const useTheme=()=>useContext(C);