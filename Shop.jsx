import React,{useMemo,useState} from 'react';
import ProductCard from './ProductCard.jsx';
import {products,categories} from './products.js';
export default function Shop(){const [filter,setFilter]=useState('all'),[q,setQ]=useState('');const shown=useMemo(()=>products.filter(p=>(filter==='all'||p.category===filter)&&p.name.includes(q)),[filter,q]);return <div className="section"><div className="section-head"><div><span className="eyebrow">Khuyoot Store</span><h1 style={{fontSize:42,margin:'12px 0'}}>كل المنتجات</h1><p style={{color:'var(--muted)',fontSize:12}}>خيوط، صوف، أدوات كروشيه وإكسسوارات مختارة بعناية.</p></div><div className="search-wrap"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="بحث..."/></div></div><div className="filters">{categories.map(c=><button key={c.id} className={filter===c.id?'active':''} onClick={()=>setFilter(c.id)}>{c.label}</button>)}</div><div className="products-grid">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div></div>}
