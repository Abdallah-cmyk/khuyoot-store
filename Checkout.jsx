import React,{useState} from 'react';
import {Link} from 'react-router-dom';
const KEY='khuyoot_cart', WA='201091321859';
export default function Checkout(){const items=JSON.parse(localStorage.getItem(KEY)||'[]');const [form,setForm]=useState({name:'',phone:'',city:'',address:'',notes:''});const total=items.reduce((s,x)=>s+(Number(x.price)||0)*(x.quantity||1),0);const submit=e=>{e.preventDefault();if(!items.length)return;const lines=items.map(x=>`• ${x.name} × ${x.quantity} — ${x.price} ج.م`).join('\n');const msg=`طلب جديد — خيوط ستور\n\n${lines}\n\nالإجمالي: ${total} ج.م\n\nالاسم: ${form.name}\nالهاتف: ${form.phone}\nالمدينة: ${form.city}\nالعنوان: ${form.address}\nملاحظات: ${form.notes||'لا يوجد'}`;window.open(`https://wa.me/${WA}?text=${encodeURIComponent(msg)}`,'_blank','noopener,noreferrer')};return <div className="section"><div style={{maxWidth:850,margin:'auto'}}><span className="eyebrow">Checkout</span><h1 style={{fontSize:38,margin:'12px 0 30px'}}>إتمام الطلب</h1>{!items.length?<p>السلة فارغة. <Link to="/" style={{textDecoration:'underline'}}>ابدأ التسوق</Link></p>:<form onSubmit={submit} style={{display:'grid',gap:12}}><input required placeholder="الاسم الكامل" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required inputMode="tel" placeholder="رقم الهاتف" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input required placeholder="المدينة / المحافظة" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/><input required placeholder="العنوان بالتفصيل" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><textarea placeholder="ملاحظات إضافية" rows="4" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><div style={{padding:'18px 0',fontWeight:700}}>الإجمالي: {total} ج.م</div><button className="btn-primary" type="submit">إرسال الطلب عبر WhatsApp</button></form>}</div></div>}
